test:
  http_cache:
    - resource_hosts:
        - "www.example.com"
        - "www2.example.com"
      cache_locations:
        - "localhost:80"
        - "localhost2:80"

empty:
  http_cache:

empty_http_cache:
  http_cache: []

